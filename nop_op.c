#include "monty.h"

/**
  *nop_op - function that does nothing.
  *@stack: double pointer to the head of the stack
  *@line_number: current line in the bytecodes
  */

void nop_op(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
